sheet_id: 1pLN46ZKWJIsidswMeoxhZwoacuFMR08sCaTFG6mLytc
tab_name: bot_config

headers (row 1):
A: bot_mode        # bot | webhook
B: workflow        # wf1 | wf2 | wf21
C: target_group    # used by runtime when bot_mode=bot
D: group_id        # auto-managed inventory output
E: bot_name        # auto-managed inventory output (workflow tag)
F: app_id
G: app_secret
H: signing_secret
I: webhook_url     # used when bot_mode=webhook

notes:
- Runtime workflows read routing/auth from A:C + F:I.
- D:E is fully replaced by sync job output each run.
